@page "/createorder"
@inject NavigationManager NavigationManager
@rendermode InteractiveServer

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/cart", replace: true);
    }
}
